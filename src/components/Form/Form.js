import React from "react";

import StyledForm from "./Form.styled";

import Button from "../Button";

const Form = () => {
  return (
    <StyledForm>
      <Button>Dalej</Button>
    </StyledForm>
  );
};

export default Form;
